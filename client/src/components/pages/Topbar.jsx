import { ButtonLogo } from '../buttons'

const Topbar = () => {
    return (
        <nav className="w-full flex justify-center items-center p-4">
            <ButtonLogo />
        </nav>
    );
}

export default Topbar;